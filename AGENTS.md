# AGENTS.md

## Project

Comparative analysis between U-Net (CNN) and SegFormer (ViT) for semantic segmentation of coffee crop imagery. Python. MIT license.

## Status

Early-stage — no code, dependencies, or tooling configured yet. The `.gitignore` is Python-focused and references uv, ruff, mypy, pytest, Jupyter, Streamlit, and Marimo as expected tools.

## Conventions (once set up)

- Use `uv` for package management (implied by `.gitignore`).
- Lint with `ruff`, typecheck with `mypy`, test with `pytest`.
